# This will override the default generated page when we have a folder with markdown files.

Do what you want here like generate a link to the files in the nested_with_index folder or not, you're overriding the default behavior.
